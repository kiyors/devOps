# LocalStack Environment Variables

# Services to load at startup (comma-separated)
# Restrict this to only what you need to save RAM (e.g. s3,iam,lambda)
SERVICES=s3,iam,lambda,apigateway,ec2

# Enable debug logging (1=true, 0=false)
DEBUG=0
